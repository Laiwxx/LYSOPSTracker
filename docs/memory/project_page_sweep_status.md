---
name: Page-by-page pre-launch sweep status
description: Which pages have been bug-swept vs. still pending, so sessions don't re-audit stable pages
type: project
originSessionId: 3982e62d-8a2e-4377-ad60-766b73859530
---
Lai is doing a page-by-page pre-launch sweep of ops-tracker. Track which pages are considered stable so future sessions don't re-open settled work.

**Why:** he scopes work by page, not by feature, and wants to close pages one at a time before launch. Re-auditing a "done" page wastes tokens and risks regressions.

**How to apply:** before diving into a page, check this file. If it's listed as stable, ask before re-auditing. If it's on the pending list, that's fair game.

## Stable as of 2026-04-15
- **Team page** (`/my-tasks`, public/my-tasks.html) — swept 2026-04-15 via senior-engineer + direct fixes. Closed: ghost-task validation (blocked save w/o assignee), double-submit guard, fetch error handling w/ alert-on-failure, self-email + self-calendar skip when createdBy===assignedTo, sendEmail 429 retry w/ Retry-After + exponential backoff, morning briefing serialized (was Promise.all → throttled), new "Requests You Sent" section w/ Awaiting/Seen/Done pills. Treat as locked unless Lai reports a new bug.

## Factory page — Bundle 1 + Phase 1 + Phase 2A shipped 2026-04-15 (NOT yet stable — Phase 2B+ pending)
- **Bundle 1 (factory-side data plumbing):** FAB_WRITABLE_FIELDS whitelist on narrow PUT, POST append route, autoDeriveFabStatus for batch items, applyFabDerivations shared helper, client saveFabField helper, updateFabItemStage + saveSingleItem rewritten to narrow routes. Closed lost-update race, spurious DR emails, server-managed field injection, stale status chips.
- **Phase 1 (site-request unification):** retired `project.deliveryRequests[]` entirely. Deleted Trigger 1 email, PUT delivery-requests route, buildDefaultProject field, empty arrays in projects.json (15 projects). Noon cron rewritten to read site-requests via `getRoleEmail('Factory Manager')` (new helper). Factory-queue deliveryMap now sourced from site-requests.json filtered by projectId. /project Delivery Requests tab rebuilt as read-only site-requests view (`renderDeliveryRequestsTab` async + `buildSiteRequestCardReadonly`); old edit/save/delete handlers + ticketBadgeHtml/buildAuditHtml/notifyDelivery/buildDeliveryRequestCard all removed.
- **Phase 2A (data foundation for shipment tracking):** `fabIdx` column added to site-request schema. POST /api/site-requests accepts + validates fabIdx, enforces projectId existence (closes bug #10), switched email notification from hardcoded 'Chris' to `getRoleEmail('Factory Manager')`. /installation dropdown captures fabIdx from `proj.fabrication` rows via option dataset; scope-only and "Other" items send `fabIdx:null`. Factory-queue now computes per-row `qtyShipped` (sum of Delivered SRs keyed by fabIdx first, falls back to item-name for legacy/free-text) and `qtyOnFloor = max(0, qtyDone - qtyShipped)`. Legacy SR `mnx7ic3ro44b` (the 1 existing record, "SP400" → fab row 3 "SP400 Fixed Bollard Shallow Mounted") backfilled via fuzzy name match — illustrates exactly the name-drift problem fabIdx solves.
- **Phase 2B (factory card UX):** Factory page now renders `· X shipped · Y on floor` beside the built counter on every fab row (display-only, suppressed when both are 0). Stamping happens client-side in loadFactoryQueue by merging queue items' qtyShipped/qtyOnFloor into fp.fabrication[idx]._qtyShipped/_qtyOnFloor. Stats chip replaced: was "N 📦 ready" (summed `r.readyForDelivery`), now "N 📦 on floor" (sums `_qtyOnFloor` across active projects). **Factory-side `readyForDelivery` use retired**; the field still lives on in the schema + in /project.html's Mark Ready button + project.js `buildFabRow` UI — those retire in the /project sweep (Phase 4), NOT now, because removing them without a replacement UI is a regression.
- **Phase 2C (soft-warn guardrail):** Server computes `qtyOverShipped = qtyShipped > qtyDone` on every factory-queue item. Factory.html stamps it onto `_qtyOverShipped`, `renderFabItems` shows a red `⚠ over-shipped` pill on the shipment line. Non-blocking — Chris fixes the source SR quantity or built counter. Revealed live on Nakano SP400 which had `quantity:60` on the one Delivered SR vs `qtyDone:22` on the fab row; SR quantity corrected to 22 by the boss as test data cleanup.
- **Phase 2D (Bundle B.5 — index integrity + error surfacing):** Senior-engineer sweep flagged four issues post-Phase-2 and all shipped:
  - **HIGH #1 (fab-row delete orphan):** new narrow `DELETE /api/projects/:id/fabrication/:idx` route splices the fab row AND walks site-requests.json for that project, nulling `fabIdx` on SRs whose fabIdx matched the deleted row and decrementing fabIdx on SRs below it. Returns `{srReindexed, srNulled}` counts for toast feedback. Client `del-row-btn` in public/js/project.js now calls the narrow route and shows a confirm dialog + success toast with counts. Closes the time-bomb where Bundle C's split siblings would've inherited silently-corrupt fabIdx on any project with a mid-life fab-row delete.
  - **HIGH #2 (scope-only shipment display gap):** factory.html else-branch in loadFactoryQueue now synthesizes an fp.fabrication[idx] row when `it.fromScope` and the target index is past fabrication.length (scope-only items are appended by the server in `fabItemsRaw.concat(scopeOnly)`). Scope-only rows with a legacy name-matched Delivered SR now correctly render the shipment line.
  - **MED #3 (silent save failures):** `updateFabItemStage` and `saveSingleItem` catch blocks now call `factoryToast(msg, false)` with the server error on top of the existing red-chip/❌ flash. Original bug #1-3 from the first Factory sweep is closed.
  - **MED #4 (rate limits):** Added `postRateLimit` middleware to `PUT /api/site-requests/:id` and `DELETE /api/site-requests/:id`. Original bug #11 closed.
- **Phase A (daily-log data model):** logs[] array on every fab row. qtyDone became a derived cache (sum of logs[].delta), recomputed by new `recomputeQtyDone()` helper inside `applyFabDerivations`. New routes POST/PUT/DELETE `/api/projects/:id/fabrication/:idx/logs` — all rate-limited, all enforce non-zero delta + non-empty photoPath, all hard-block over-BOM and under-shipped. Edits preserve prior versions in `editHistory[]`, `editedAt`/`editedBy` stamped. Migration script `scripts/migrate-fab-logs-baseline.js` seeded 3 existing non-zero rows with synthetic baseline entries (loggedBy='system', photoPath=null, note='Pre-launch baseline'); all 24 zero rows got empty logs[] for schema consistency. 10/10 smoke tests green. **Dual-path known gap:** /project.html buildFabRow still accepts direct qtyDone writes via narrow PUT (old code path); writes bypass logs[]. Documented; resolves when /project fab UI retires in Phase 4.
- **Phase B (photo hygiene infrastructure):** added sharp 0.34.5 dep (libvips 8.17.3, HEIC supported via libheif). New `uploadLogPhoto` multer memory-storage instance. New route `POST /api/projects/:id/fabrication/:idx/log-photo` runs the uploaded image through sharp pipeline (auto-rotate from EXIF → resize-inside 1600px → progressive JPEG quality 85 mozjpeg → implicit EXIF strip). Writes to `public/uploads/fab-logs/<projectId>/<logId>.jpg`. Returns `{photoPath, logId}`. Accepts caller-supplied logId so the client can pre-generate and pair the photo to a later log-create call. Log-create route now accepts optional `id` in body (validated `/^log_[a-z0-9_]+$/i`, rejects duplicates with 409). 9/9 smoke tests green: EXIF stripped, dims capped, JPEG normalization, duplicate id rejected, bad id format rejected, non-image rejected. Orphan cleanup (delete photo file when log entry deleted) still deferred — TODO in DELETE log route comment; Phase C delete UX leaves photo files behind on disk, not yet load-bearing. Backup to Hostinger: deferred per boss call (uploads/ empty; revisit when accumulation starts).
- **Phase C (daily-log UX — row rebuild + log modal + edit/delete inline):** Rewrote `renderFabItems` from the old ±counter+save+photo row into a 3-section clean layout: head (name + stage chip), state (qtyDone/total + shipment line + over-sent pill), action (Log today button OR logged-today split button + Log again). Deleted `saveSingleItem`, `uploadItemPhoto`, and `updates[]` global tracker — all superseded by the log model. Added full log modal with camera capture (capture="environment"), client-side `compressImage()` via canvas.toBlob (1600px / 0.85 quality), `uploadLogPhotoBlob` + `createLogEntry` helpers. Modal binds once, resets on every open, busy-locks save button during upload, surfaces inline errors without losing input. **Edit/delete wired in the same bundle** (after boss called out the gap — saved as `feedback_ship_complete_workflows.md`): tapping "✓ Logged today" chip expands an inline panel showing today's log entries with time · delta · photo thumb · note · Edit/Delete buttons. `openLogEditModal(pid, idx, logId)` opens the same modal pre-filled, photo optional (keep existing or replace, with same logId → photo file overwrites in place). `saveLogEntry()` dispatches create vs PUT edit by `logModalState.editingLogId`. `deleteLogEntry()` confirms → DELETE → row re-renders.
- **Phase C finalize (boss pushback on "API reflects logs" + closure gaps):** Closed 4 gaps before the phase was truly done:
  1. **`readProjects()` now normalizes `qtyDone` from `logs[]` sum on every read**, so any API response reflects the logs-derived truth, not a stale cache. Rows with empty logs[] keep their existing qtyDone (backward compat with /project.html direct edits).
  2. **`/api/factory-queue` items include `logs[]`** so the UI can render the today's-logs panel from one network call.
  3. **Photo orphan cleanup on DELETE log**: the DELETE route now unlinks the photo file from disk with a path-traversal guard (resolved absolute path must live inside `UPLOADS_DIR`). Returns `photoUnlinked: true|false` in the response. Non-fatal on failure — logs error but still deletes the entry.
  4. **Fat-finger confirm on big deltas** in `saveLogEntry` client side — native `confirm()` when `|delta| >= 10 AND |delta| >= 20% of totalQty`. Catches "+30 instead of +3" class before server hit. User can still proceed if intentional.
- **Phase C finalize — the dual-path bug that almost killed Phase C silently:** The full-project PUT handler (`PUT /api/projects/:id`) does a shallow spread `{ ...oldProject, ...incoming }`. When `/project.html`'s `buildFabRow` sync writes fab fields and calls `debouncedSave()`, the payload does NOT include `logs[]`, so the spread would REPLACE the server-side fabrication array and wipe every log entry on every save. This became load-bearing the instant Phase C shipped because live logs were being created and any parallel /project.html edit would silently destroy them. **Fix:** the PUT handler now walks `incoming.fabrication[]` before the spread; for each row, if fields are absent (`logs`, `fab_started_at`, `fab_completed_at`, `cycle_days`), it copies them from the matching old row (matched by item-name first, index fallback). Verified via smoke test: client sends project with `logs` explicitly deleted → server preserves logs[] on disk. Closes the known dual-path gap for the log field specifically; the `qtyDone` write path from /project still exists as documented technical debt until Phase 4 retires the /project fab editor.
- **Phase C smoke test results:** 6/6 finalize checks + 7/7 earlier end-to-end checks + dual-path regression test all green. Migration ran a second time during finalize to re-seed 2 baselines (SP30, Cova Sliding Gate) that had been wiped by earlier parallel edits before the dual-path fix landed — data backup at `data/projects.backup-fab-logs-1776274365452.json`.
- **Delete-last-log bug fix (shipped alongside Phase D):** `recomputeQtyDone` used to early-return on empty `logs[]`, which meant deleting the only log entry left `qtyDone` stale at its prior value (user deletes the SP30 baseline → row still shows 127 instead of 0). Flipped the rule: if `logs` is explicitly an array (possibly empty), normalize `qtyDone` to sum(deltas) = 0 for empty array. Rows with no `logs` field at all are still left alone (defensive for brand-new rows created after migration, though the dual-path PUT fix means this shouldn't happen). Verified: deleting the only log on Cova Road Blocker correctly zeroes qtyDone on both the API response and `readProjects` normalization path.
- **Phase D (historical log view in inline panel):** Renamed `renderTodaysLogsHtml` → `renderLogHistoryHtml`. Panel now shows ALL logs for a row (not just today's), sorted newest first, grouped by SGT calendar day with date headers (`Today · 15 Apr` highlighted in accent color, past days show `15 Apr 2026`). CSS updated: `max-height: 320px` + `overflow-y: auto` for scrollable long histories, new `.fab-log-day-header` class. Chip label in `renderFabItems` adapts to state: when something was logged today, shows `✓ Logged today (+3)` in green-ish and the sibling button is `+ Log again`; when the row has logs but none today, shows `📋 History (N)` and the sibling button becomes `📷 Log today`; when no logs at all, single big `📷 Log today's build` button (Phase C state unchanged for empty rows). Every entry in the panel — today OR historical — carries Edit/Delete buttons wired to `openLogEditModal` and `deleteLogEntry`. Phase C's "historical edit deferred" limitation is CLOSED. 8/8 Phase D smoke tests green: delete-last-log zeroes qtyDone, date grouping, factory-queue returns full history, renderLogHistoryHtml helper present, old renderTodaysLogsHtml helper removed, cleanup + baseline restore.
- **Phase E (2026-04-17 — force-delete + stage chip fix):** (1) Log delete force-override: when deleting a log would drop built qty below shipped floor, server now accepts `{ force: true, reason }` — proceeds but logs as `fab.log.force-deleted` with full audit trail (overrideReason, shippedFloor, deletedDelta). Client prompts for reason via native prompt(). (2) Stage chip bug: `autoDeriveFabStatus` was overriding manual stage advances (e.g. tapping Not Started → In Progress on qty=0 snapped back). Fix: PUT route passes `skipStatusDerive` when client sends explicit status; log-based flows still auto-derive. (3) Cleared test SR data (legacy `mnx7ic3ro44b`). (4) Full button/interaction audit done — 29 interactions traced.
- **Still pending on Factory page (in priority order):**
  1. **Hardcoded "Chris" → role-based** (5 spots: deleteSR actor, srUpdate _actor, submitPR submittedBy, toast "Rena notified", display "Chris:" before notes, `chrisNotes` field name)
  2. **Double-click guards** on SR buttons (Ack/Flag/Mark status/Resume/Delete) and stage chip advance/back — no busy flag, rapid clicks fire duplicate fetches
  3. **Dead code cleanup** — `collectAllFabPhotos()` (line ~1740) never called
  4. **Empty states** — SR section shows blank on fetch fail; init fetch failure → "Loading..." forever
  5. **Stats chips rework** — current chips are basic count-only
  6. **Priority banner rewrite** with why-urgent reasoning
  7. **Partial-send modal** for split route
  8. **Over-sent hard-block UI** (currently pill-only)
  9. **SR section collapse**
  10. **Inline styles → classes** (78 inline `style=` occurrences)
  11. **Log delta input** — HTML `min="1"` conflicts with allowing negative corrections
  12. **Bug-sweep leftovers:** #4 double-click SR email storm, cron noon `status==='New'` improvement for split siblings. (#6 DO upload res.ok check looks already fixed; #7 submitFactoryPR validation looks addressed — confirm on next session)
  13. **Hostinger backup for uploads/** — deferred

## Pending (not yet swept)
- Dashboard (`/` index.html)
- Installation (`/installation`)
- Procurement (`/procurement`)
- Manpower (`/planning`)
- Attendance (`/attendance`)
- Admin (`/admin`)
- New Project (`/new-project`)
- Project (`/project` — read-only consolidation)
- Tasks (`/tasks` — global board)
- Feedback (`/feedback`)

## Deferred cross-cutting
- **Email HTML template refactor** — explicitly deferred to the LAST polish pass, after all pages are stable. Consolidate the ~8 inline templates in server.js into one `renderEmail({title, rows, ctaUrl, ctaLabel})` helper. Don't touch email HTML mid-sweep — any logic change re-churns the template.
- **Senior-engineer review leftovers on Team page** (minor, not blockers): #7 background /api/staff refresh clobbers nt-requested-by, #9 server-side taskType whitelist, #8 tags array sanitization, #11 client 'Medium' vs server 'Normal' priority default mismatch. Pick up during the final polish pass.
